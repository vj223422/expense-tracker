package com.expensetracker.app.core.theme

import androidx.compose.material3.Typography

// System-font default (no bundled font asset) — keeps the APK lean; the M3 default scale
// already covers every role the app's screens use.
val ExpenseTrackerTypography = Typography()
