# Copy this file to release.keystore.properties (git-ignored) and fill in your own real release
# keystore before running ./gradlew assembleRelease / bundleRelease. Never commit the real file or
# the keystore itself — whoever holds this key controls every future update to the app.
storeFile=release.jks
storePassword=changeme
keyAlias=release
keyPassword=changeme
